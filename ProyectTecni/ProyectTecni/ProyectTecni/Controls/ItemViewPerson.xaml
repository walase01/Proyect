<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectTecni.Controls.ItemViewPerson">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}" Style="{StaticResource Key=LabelStyleList}" ></Label>
        <Label Grid.Row="1" Grid.Column="0" Text="{Binding LastName}" Style="{StaticResource Key=LabelStyleList}" ></Label>
        <Label Grid.Row="0" Grid.Column="1" Text="{Binding Cedula}" Style="{StaticResource Key=LabelStyleList}" ></Label>
    </Grid>
    

</StackLayout>